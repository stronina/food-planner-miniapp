<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Food Planner</title>
<style>
  body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: #121212; color: #fff; }
  nav { display: flex; background: #1f1f1f; }
  nav button { flex: 1; padding: 10px; border: none; background: #1f1f1f; color: #fff; font-size: 14px; cursor: pointer; }
  nav button.active { background: #333; }
  .tab { display: none; padding: 20px; }
  .tab.active { display: block; }
</style>
<script src="https://telegram.org/js/telegram-web-app.js"></script>
</head>
<body>

<nav>
  <button id="tab-today" class="active">Сегодня</button>
  <button id="tab-week">Неделя</button>
  <button id="tab-history">История</button>
</nav>

<div id="today" class="tab active">
  <h2>План на сегодня</h2>
  <p>Завтрак: пресет 1</p>
  <p>Обед: пресет 2</p>
  <p>Ужин: пресет 3</p>
  <p>Перекус: пресет 4</p>
  <p>Итого: 1500 ккал</p>
</div>

<div id="week" class="tab">
  <h2>План на неделю</h2>
  <p>Здесь будет автогенерация меню на 7 дней.</p>
</div>

<div id="history" class="tab">
  <h2>История</h2>
  <p>Выберите день, чтобы посмотреть подробности.</p>
</div>

<script>
  // Telegram Mini App API
  const tg = window.Telegram.WebApp;
  tg.ready();
  tg.expand();

  // Tabs
  const tabs = document.querySelectorAll("nav button");
  const tabContents = document.querySelectorAll(".tab");

  tabs.forEach((btn, idx) => {
    btn.addEventListener("click", () => {
      tabs.forEach(b => b.classList.remove("active"));
      tabContents.forEach(c => c.classList.remove("active"));
      btn.classList.add("active");
      tabContents[idx].classList.add("active");
    });
  });
</script>

</body>
</html>
